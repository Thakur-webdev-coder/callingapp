import { StyleSheet } from "react-native";
import colors from "../../../assets/colors";

export default StyleSheet.create({
  textStyle: {
    color: colors.white,
    fontSize: 15,
    alignSelf: "center",
    textAlign: "left",
  },
});
